MODULE = Ogre     PACKAGE = Ogre::ColourValue

ColourValue *
ColourValue::new(red=1, green=1, blue=1, alpha=1)
    float red
    float green
    float blue
    float alpha

void
ColourValue::DESTROY()
